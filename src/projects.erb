---
layout: default
title: Projects
---
<section>
  <h2>Projects</h2>
  <% site.data.projects.each do |project| %>
    <h3><%= link_to "#{project.name} (#{project.type})", project.link, target: '_blank' %></h3>
    <p><%= project.description %></p>
  <% end %>
</section>